# Campaign of 2 May 2026: what these numbers are, and what they are not

These files are the original output of the validation campaign, preserved as
they were produced. **They were not generated by the current revision of this
repository.** They came from the earlier sweep tooling that lived outside Git
(`Yash-New-Computer-Testsweeps/scripts/run_maximal_p_sweep.py`), run on
2 May 2026. They are included so the results can be inspected and checked, not
as output you can regenerate byte-for-byte by running the code here today.

## What the campaign covered

- 9 generators from the modified NIST STS, 1 GB each, listed with their SHA-256
  in `../input-manifest.csv`
- Tests **22 to 31 only**, 10 tests per generator, 90 rows
- XOR mode, etalon `etal.bin` (40 GB)
- Detection threshold 1e-10
- Default KS routine, not `-k`

Tests 32 to 41 were **not** part of this campaign. In particular test 36 is
absent, so the counting error corrected in `robust/test_nonperiodic.c` (see
`../../CHANGES-vs-upstream.md`) does not touch any number in `maximal_p.csv`.

## Result

| Generator | Detections (of 10 tests) |
|---|---|
| Blum-Blum-Shub *(control)* | 0 |
| Linear Congruential *(control)* | 0 |
| Micali-Schnorr *(control)* | 0 |
| Cubic Congruential | 3 |
| G-using-SHA-1 | 6 |
| Modular Exponentiation | 2 |
| Quadratic Congruential I | 2 |
| Quadratic Congruential II | 3 |
| XOR | 5 |

## Known defect in the tooling that produced this, and what was checked

The old sweep matched numbers in `rtest` output with a pattern that also
matched bare integers. On end of input, `rtest` prints

    oops, eof in generator 0

and the pattern picked up the generator number `0`, recorded it as a p-value of
0.0, and labelled the probe `eof_zero`. The `bad_reason_at_ceiling` column
therefore conflates two different things, and its `eof_zero` and `parse_error`
values should both be read as "the sweep stopped here", not as a measured
result.

This matters because a genuine p-value below the printing floor would also read
as 0.0, and would have been discarded the same way. So the ceilings were
checked directly rather than assumed:

- 11 rows have `eof_zero` at the ceiling. All are test 23, all stop at the same
  `max_p` of 1953, across every generator, which is a property of the file size
  and the test's consumption rate rather than of any generator.
- The boundary was rerun on the original inputs with the current code:

      rtest -x -f bad_Blum-Blum-Shub_1GB.bin -e etal.bin -p 1953 -q 1953 -d 1 -n 0 -t 23 -r 1
      -> 0.261534995558913019          (matches the recorded 0.261534995558913)

      rtest -x -f bad_Blum-Blum-Shub_1GB.bin -e etal.bin -p 1954 -q 1954 -d 1 -n 0 -t 23 -r 1
      -> oops, eof in generator 0

  So the ceiling is genuine input exhaustion, and the recorded value at the
  last good sample size reproduces exactly on today's code.

The current tooling fixes both halves of this: it reads end of input from
`rtest`'s own message before parsing, keeps a p-value below the printing floor
as `censored_zero` rather than discarding it, and records the numeric status and
the KS backend in its output. See `scripts/run_maximal_p_sweep.py`.

## What has not been established

- The generator files have **not** been rebuilt from
  `../generators/NIST_STS_Modifications_Guide.md` and compared against the
  hashes in `../input-manifest.csv`. Until someone does that, the recipe is
  documentation, not a verified reproduction path.
- Tests 32 to 41 have no campaign of their own.
- The reported minimum over a sweep is a search summary, not a calibrated
  p-value. See the note in the main README.
